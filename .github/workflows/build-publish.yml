name: Build & Publish Docker

on:
  push:
    tags:
      - 'v*'

jobs:
  build-publish:
    uses: imapps/utils/.github/workflows/build-publish.yml@main
    with:
      services: '["kivo"]'
      is-monorepo: false
    secrets:
      DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
      DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
